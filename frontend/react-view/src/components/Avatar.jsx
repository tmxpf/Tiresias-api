import React from 'react';
import { Button } from 'reactstrap';

const Avatar = () => {
    return (
        <>
            <Button outline color="success">로그인</Button>
            <Button outline color="success">회원가입</Button>
        </>
    );
};

export default Avatar;
